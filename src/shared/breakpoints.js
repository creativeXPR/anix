// Keep these in sync with the hardcoded @media values in CSS files — see
// use-viewport-tier.js for the JS side of the same two boundaries.
export const TABLET_MIN = 600
export const DESKTOP_MIN = 900
